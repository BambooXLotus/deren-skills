# Versions: rick-review

Append-only history of /rick-improve runs on this skill. Each entry is one bounded loop.

- v1 | 2026-06-11 | pre-loop snapshot
- v1 | 2026-06-11 | 3-rounds
  - Round 1: closed two vague-enforcement holes in Step 6/7.5 — P3 verification row (line 172) now has a concrete kill trigger (`cited file isn't in /tmp/rr-files.txt`) instead of "kill on suspicion of hallucination," and Step 7.5 Hedge scan (line 213) drops the unenforceable "unless prior reviews accepted the same pattern" exception so P3 hedges follow the same symmetric kill rule as Check 3.
  - Round 2: clarified DOWNGRADE mark (line 191) is P0-only since lower tiers don't read code, blocking intuition-driven severity adjustments at P1/P2/P3; added explicit verdict rule at top of Step 8 — `NEEDS FIXES if any P0 or P1 survives Step 7.5, otherwise APPROVED` — so the verdict printed to chat and VERSIONS.md is deterministic instead of model-judgment.
  - Round 3: fixed Step 2 branch-list inconsistency (line 51) — PR-number fallback now includes `master` to match Step 1's `{main, master, develop}` exclusion, preventing PR-mode reviews on master-default repos from silently falling through to the date-named folder.
  - Restore pre when: the round 1 hedge-scan tightening kills a real P3 finding that turned out to matter in a follow-up PR (the "unless prior reviews accepted" escape hatch existed for a reason, even if unenforced); or the round 2 verdict rule produces a NEEDS FIXES verdict on a P1 that everyone agrees shouldn't block merge (rule too strict).
  - Restore post when: a master-default repo runs /rick-review on a PR and the folder name correctly resolves to pr-<N> instead of review-<date>; OR a P3 finding survives Step 6 with the new concrete kill trigger that the old "suspicion" wording would have killed (showing the trigger is well-calibrated); OR a model emits a deterministic verdict that the previous wording would have made model-judgment.
- v2 | 2026-06-11 | pre-loop snapshot
- v2 | 2026-06-11 | zero-changes
  - Round 1: rewrote the TRUNCATED marker emitted by `shrink()` (line 116) so agents past the cut cite from `/tmp/rr-files.txt` and don't flag "missing X" (kills the context-budget leak where an agent honored the old "read files directly" wording and loaded full files past the cap); converted the four conditional triggers in the Step 5 routing table from prose hints to runnable shell tests — Pickle Rick / Scientist Rick / Evil Morty became `[ -s ... ]` membership checks, Rick Prime became a `grep -qE` over the broad slice plus a name-only check for new top-level `src/<dir>/`.
  - Round 2: tightened the Rick Prime regex introduced in round 1 from `^\+.*from .{1,4}\.\./[^.]` to `^\+(import\|export).*from .*\.\./` — anchors on the import/export keyword (eliminates false positives from JSX props like `<X from="../old" />` and added comments), drops the `.{1,4}` quote-glue (matched any 1–4 chars, not just quotes), and drops the `[^.]` tail (which silently excluded legitimate `../../` deep cross-module imports).
  - Round 3: stopped early — no improvements found this round. Remaining candidates (Step 1's unused no-arg `gh pr view`, Step 6 P2 vs P3 wording overlap, Step 5 model-column paragraph length) failed the "name the specific failure mode" check; the no-arg probe can't be misused by an orchestrator that honors Step 1's "PR mode only" gate, P2 vs P3 wording overlap doesn't change the effective kill rule, and the model-column rationale is load-bearing for the reader.
  - Restore pre when: the new Rick Prime regex misses a real cross-module import that the old `from .{1,4}\.\./[^.]` form would have caught (the round-2 anchoring on `^\+(import|export)` over-narrowed past actual TS patterns); OR an agent encounters the new TRUNCATED marker and skips a real finding it would have caught by loading the file directly (the round-1 wording over-disciplined the agent); OR a conditional trigger's runnable shell test silently fails (e.g. `[ -s ]` returns false because `/tmp/rr-diff-data.txt` was never created when the data slice happened to be empty) and an agent that should have run is skipped.
  - Restore post when: an orchestrator runs the new shell-test triggers and routes Pickle / Scientist / Evil Morty deterministically where the prose hints previously left the routing decision to model judgment; OR a diff truncates and the agent honors the new marker (cites from `/tmp/rr-files.txt`, no "missing X" false flag, no full-file read past the cap); OR a PR adds JSX with `from="../old"` and Rick Prime correctly does NOT fire (the round-2 regex anchor prevents the false trigger the round-1 regex would have produced).
- v3 | 2026-06-11 | pre-loop snapshot
- v3 | 2026-06-11 | 3-rounds
  - Round 1: tightened Step 7.5 Check 2 hedge-scan trigger list (line 213) — dropped "could silently" and "after a refactor" (present-tense consequence language describing real-today bugs), kept only inherently prospective phrases ("if a future edit," "in case someone changes," "for symmetry," "for parity," "for completeness," "hypothetical"), and added a guard sentence telling future edits not to re-add present-tense phrases. Failure mode this closes: a real P1 written in plain consequence prose like "query could silently return stale data under concurrent writes" was auto-demoted to P2, which flipped the Step 8 verdict from NEEDS FIXES to APPROVED.
  - Round 2: replaced Rick Prime's second trigger (line 144) from `git diff $BASE_REF --diff-filter=A --name-only \| grep -qE '^src/[^/]+/[^/]+$'` to `comm -13 <(git ls-tree $BASE --name-only src/ \| sort) <(git ls-tree HEAD --name-only src/ \| sort) \| grep -q .`. The old regex matched any added file at exactly two segments under `src/` (e.g. a single new `src/components/Button.tsx`), false-firing Rick Prime on the `inherit` model for every routine PR adding a component file; the new comm-based test compares top-level `src/` entries at BASE vs HEAD, firing only when a new top-level dir is actually added, which is what the parenthetical "(new top-level `src/` dir)" already promised.
  - Round 3: tightened Step 7 dedupe rule (line 197) from "same `file:line`" to a three-part conjunction — (a) file matches, (b) line ranges overlap or are equal (treat `47` and `47-50` as overlapping), (c) Issue text names the same defect. Failure modes closed: two agents flagging the same line for different defects (e.g. "missing await" vs "unsafe cast" at `auth.ts:47`) were silently merged and one finding dropped from the report; agents flagging the same defect with different line-range strings (`47` vs `47-50`) failed to dedupe, missed the `**` cross-cutting flag, sorted below `**` rows in Step 7, and lost Step 7.5's auto-keep that would have protected them from hedge demotion.
  - Restore pre when: the round 1 hedge-scan tightening lets a genuinely prospective P1 written with the word "after" survive Check 2 when the v2 list would have caught it (showing the new list under-catches); OR the round 2 comm-based test misses a real architectural reshuffle inside an existing top-level `src/` dir (e.g. `src/components/` gaining `src/components/forms/`) that the old regex would have flagged; OR the round 3 dedupe rule produces too many separate rows because agents phrased the same defect with different nouns ("missing await" + "unawaited promise") and the user complains about duplicate-looking findings.
  - Restore post when: a real P1 written with the phrase "could silently" survives Check 2 and ships with NEEDS FIXES (the v2 list would have demoted to P2 and approved); OR a PR adds a single new file under existing `src/components/` and Rick Prime correctly does NOT fire (saving the inherit-model run) while a separate PR that creates `src/payments/` correctly does fire; OR two agents flag the same line for different defects and both appear in the report instead of one silently merging, AND two agents flag the same defect with `47` vs `47-50` line specs and the `**` flag attaches.
- v4 | 2026-06-11 | pre-loop snapshot
- v4 | 2026-06-11 | zero-changes
  - Round 1: closed two vague-enforcement holes — Step 6 grep block (line 174) now requires "one pattern per finding, derived from the cited code snippet or named symbol — not a loose keyword like the function name alone," preventing the orchestrator from grep'ing `save` and killing every real missing-await finding by over-matching; and Step 7.5 re-numbering line (217) now explicitly states the tier counts Step 8 prints to chat and writes to VERSIONS.md are post-Step 7.5 numbers, blocking the contradictory `P0:1, Verdict: APPROVED` output mode where chat counts could leak Step 7's pre-kill aggregate.
  - Round 2: anchored the Step 6 Pre-existing code filter (line 163) to the diff-slice prefix instead of an abstract "existed identically on $BASE" check — orchestrator now verifies in-scope by locating the cited line as a `+` line (added) or `-`/`+` pair (modified), and kills it as out-of-scope if it's a context line (no prefix) or absent from the slice. Closes the gap where orchestrator either burned tokens on `git show $BASE:file` round-trips or eyeballed the cite against diff memory and silently kept pre-existing findings.
  - Round 3: stopped early — no improvements found this round. Remaining candidates (Step 1's no-arg `gh pr view` informational query at line 24, Step 2 priority 2 being unreachable through Step 1's branch-mismatch gate when on main/detached HEAD, Step 8 line 70's pre-agent mkdir of `agents/v<N>/`) failed the "name the specific failure mode" check; line 24 still feeds the model context even if unbound, the Step 2 / Step 1 interaction needs a structural redesign rather than a tighten, and the early mkdir is defensive design with no downside.
  - Restore pre when: the round 1 grep-pattern tightening rules out a legitimate symbol-level pattern that the old "covering every citation" wording would have allowed (e.g. agent flags an entire missing-await pattern across multiple call sites and the new "not a loose keyword" rule forces the orchestrator into per-line patterns that miss the cross-site relationship); OR the round 1 post-Step 7.5 counts rule produces a chat print where the user expected raw aggregate ("you said you killed 3 findings but the counts dropped silently"); OR the round 2 diff-slice anchoring kills a real finding cited correctly but at a context line because the agent quoted surrounding code instead of the changed line (the old "existed identically on $BASE" wording would have let a `git show $BASE:file | diff` round-trip catch the equivalent moved-but-modified case).
  - Restore post when: a P1 finding survives because the orchestrator grep'd the cited snippet instead of a function-name keyword (the round 1 guidance preserved a real finding the old wording would have killed by over-matching); OR a Step 7.5 kill happens and the chat print correctly shows the post-kill counts matching the verdict (no `P0:1 / APPROVED` contradiction); OR an agent flags `auth.ts:47` citing a context line copied from surrounding code and the round-2 prefix check correctly kills it as out-of-scope, where the old "existed identically on $BASE" wording would have required a slower file fetch to reach the same conclusion.
- v5 | 2026-06-11 | pre-loop snapshot
- v5 | 2026-06-11 | zero-changes
  - Round 1: tightened Step 3 versioning rule (line 60) — replaced "Next `N` = highest + 1" with explicit integer-parsing ("parse the integer from each `v<N>.md`, `N` = max integer + 1") plus a concrete warning that lexical sort gives `v9.md` at v10+ and overwrites the existing v10.md snapshot. Mirrors `rick-improve`'s "highest integer (ignore `-pre` suffix)" wording. Failure mode this closes: at v10+ transitions, a model running `ls v*.md | sort | tail -1` picks v9 lexically, computes next = v10, silently overwrites the existing v10 snapshot (this repo already has v10 in 63-sentry-wiring/, so the transition is live).
  - Round 2: tightened the Step 6 verification budget header (line 165) from "### Verification budget" to "### Verification budget (applied to prefix-filter survivors only)". Failure mode this closes: a fast-lane reader processing P3s by jumping straight to the table reads the P3 row ("No grep, no read. Kill if cited file isn't in /tmp/rr-files.txt") as the entire P3 regimen and skips the prefix filter; a P3 cited at a context line of an in-diff file then survives the budget's file-membership check (file IS in /tmp/rr-files.txt) but would have been killed by the prefix filter's line-prefix check. The "(mandatory)" label on prefix filter was the only signal the two layers composed; the new header makes composition explicit at the table itself.
  - Round 3: stopped early — no improvements found this round. Remaining candidates (Step 5 line 135's vague `{CLAUDE_MD_CORE}` "excerpt", Step 1/Step 4's `$BASE` shell-var vs. skill-pseudo-variable semantics, P2/P3 budget rows becoming no-ops after round 2's prefix-filter composition fix, Step 6 grep-block cross-attribution, line 54/149 rationale length) failed the "name the specific failure mode" check; CLAUDE_MD_CORE depends on per-project CLAUDE.md shape with no universal definition, $BASE substitution is a model-literacy issue not a prompt issue, redundant P2/P3 rows after round 2 are no-ops but not misleading, grep cross-attribution is mitigated by the orchestrator cross-checking match locations against citations, and the rationale paragraphs are load-bearing reader context.
  - Restore pre when: the round 1 integer-parsing rule under-specifies an edge case (e.g. a `v3-loop.md` from a prior `rick-improve` run sneaks through the "ignore -pre suffixes" rule and the integer parser fails) and the old "highest + 1" wording would have produced the right answer by lexical luck; OR the round 2 header rephrasing makes a reader believe the budget rows REPLACE the prefix filter for survivors (the round 2 wording "applied to prefix-filter survivors only" was meant to clarify composition but could be misread as "budget is the sole post-survivor check, no other policy applies"), letting a finding that should have been further gated slip through.
  - Restore post when: a project crosses the v9 → v10+ boundary on a /rick-review run (this repo's 63-sentry-wiring/ is already at v10, so the next run is live), the orchestrator parses the integer correctly per round 1's wording, and the new snapshot lands as v11 instead of overwriting v10; OR a P3 finding gets cited at a context line of an in-diff file and the orchestrator correctly applies BOTH the prefix filter and the budget row (killing it via prefix filter rather than letting the budget's file-membership check pass it), where the old "### Verification budget" header would have let a fast-lane reader skip the prefix filter and ship the parasite.
- v6 | 2026-06-11 | pre-loop snapshot
- v6 | 2026-06-11 | 3-rounds
  - Round 1: tightened Step 1 line 27's PR-mode gh invocation — replaced `gh pr view $ARGUMENTS --json ...` with `gh pr view ${ARGUMENTS%% *} --json ...` so trailing tokens (e.g. `--committed-only`) get stripped before gh sees them. Failure mode closed: `/rick-review 213 --committed-only` previously passed the whole `$ARGUMENTS` to gh, which errored on the unknown flag and aborted Step 1 (and the entire review) before scope detection ran; Step 4 line 77 already greps the full `$ARGUMENTS` separately for `--committed-only`, so the combo is a legitimate user invocation that should work end-to-end.
  - Round 2: added an explicit chained-Bash-call directive at Step 4 (new paragraph after the Auto-rule line) — "Run Step 4's three bash blocks (scope detection, slices, slice cap) as one chained Bash call — shell vars (`$BASE_REF`, `$BROAD`, etc.) set in one block don't persist across separate Bash tool calls, so splitting them empties `$BASE_REF` and silently downgrades every later `git diff` to working-tree-vs-HEAD." Failure mode closed: a naive orchestrator running each ```bash block as a separate Bash tool call gets `$BASE_REF` empty in the slice block (Claude Code's Bash tool persists CWD but NOT shell state), making `git diff $BASE_REF --diff-filter=d -- $BROAD` resolve to `git diff --diff-filter=d -- ''` which falls back to working-tree-vs-HEAD or fails outright; this is a silent-corruption failure on every invocation, with no error surfaced.
  - Round 3: tightened Step 8 line 237's VERSIONS.md bullet from "Append to ... VERSIONS.md:" to "Append to ... VERSIONS.md via the Edit tool — do NOT use Write (Write would clobber prior v1…v(N-1) review history if the payload contains only the new line):". Failure mode closed: the section header "Files to write (single batch)" and intro "All independent — write in one assistant response with parallel tool calls" frame all 4 outputs as Write operations; a model that takes the framing literally reaches for Write on VERSIONS.md, which (after the mandatory Read) overwrites the file with only the new entry, destroying the entire prior review history; the bullet now names the correct tool inline.
  - Restore pre when: the round 1 `${ARGUMENTS%% *}` expansion breaks on shells that don't support POSIX parameter expansion (rare — bash, zsh, dash all support it, but if the orchestrator's Bash tool ever runs under a stripped-down shell, the literal `%%` parses as a no-op and the gh call regresses to the v5 form); OR the round 2 chained-call instruction is misread as "always one single Bash call for ALL of Step 4 including the slice cap function definition" and a model trips over the 200+ line single block, splitting it back; OR the round 3 Edit/Write call-out is read as "always use Edit, never Write" by a model that applies the rule to the canonical/snapshot/per-agent files too, producing failed Edits on files that don't exist yet.
  - Restore post when: a user runs `/rick-review 213 --committed-only` and Step 1's gh call succeeds where v5 would have errored on the unknown flag (round 1 verified); OR a fresh orchestrator runs Step 4 by issuing each ```bash block as its own Bash tool call, notices the round 2 instruction, chains them, and produces correct $BASE-anchored slice files where v5 would have produced empty/wrong slices (round 2 verified); OR a /rick-review run completes and the VERSIONS.md file retains all prior v1…v(N-1) entries plus the new vN entry, where v5's wording made it possible for a Write-tool path to leave only the vN line (round 3 verified).
- v7 | 2026-06-12 | pre-loop snapshot
- v7 | 2026-06-12 | over-cap
  - Round 1 (trim-only, body started at 267): cut Step 2 Rationale paragraph (line 54) — restated the intro's symmetry argument + taste arguments, not operational; replaced Step 3's file layout tree (lines 62-68) with a one-line cross-reference to Step 8 + OUTPUT.md — the tree duplicated Step 8's "Files to write" list; cut Step 4 line 129's post-shrink restatement — the printf marker inside the slice file already carries the rule to the agent, and Step 6's prefix-filter handles truncated content as out-of-scope for the orchestrator. Body landed at 256, still over the 250 cap.
  - Round 2: stopped early — stop condition 3 (body exceeds 250) fired after round 1. Remaining content is operational/load-bearing per restore notes from v1-v6 (the awk dedupe explainer guards against future-editor drift; the Step 5 model-column rationale is reader context; the inline `%% *` narrative is v6 round 1's hard-won fix).
  - Round 3: stopped early — same cap condition.
  - Restore pre when: a reader of Step 3 misses the visual file-layout tree and writes outputs to wrong paths (e.g. forgets `agents/v<N>/` is nested under `review/`, dumps them at the folder root) — the one-line cross-reference may not give enough orientation; OR a future-editor refactor of the awk slurp in Step 5.5 breaks dedupe or stop-boundary semantics because Round 1 cut the explainer that called them out (NOT cut this round, but the related Step 5.5 explainer is on the edge); OR the Step 2 Rationale cut removes context that a new contributor needed to understand WHY branch-name beats PR-number in folder naming, and a follow-up PR re-introduces PR-number folders.
  - Restore post when: a reader at Step 3 follows the cross-reference to Step 8 without confusion and writes outputs to correct paths (showing the tree was visual sugar, not orientation-critical); OR Step 6's prefix filter correctly kills a finding cited past the TRUNCATED marker without needing the orchestrator-facing line 129 reminder; OR the Step 2 priority list is read and applied without the Rationale paragraph (showing the priority order itself is self-documenting).
